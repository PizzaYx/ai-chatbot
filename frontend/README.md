# 前端项目说明 (Frontend Guide)

## 📦 依赖包说明 (Dependencies)

### 核心运行时 (dependencies)
*   **vue**: `^3.4.0`
    *   Vue 3 核心框架。
*   **deep-chat**: `^2.0.0` (关键组件)
    *   开箱即用的 AI 聊天 UI 组件，自带流式解析、Markdown 渲染、输入框自适应等功能。
*   **axios**: `^1.6.0`
    *   HTTP 请求库，用于向后端发送非流式请求（如文件上传）。

### 开发工具 (devDependencies)
*   **vite**: `^5.0.0`
    *   极速构建工具，替代 Webpack。
*   **typescript**: `^5.2.0`
    *   为 Vue 提供 TS 类型支持。
*   **tailwindcss**: `^3.4.0`
    *   原子化 CSS 框架，用于快速编写布局。
*   **vue-tsc**: `^1.8.0`
    *   Vue 的 TypeScript 类型检查工具，打包时会运行。

## 🔧 核心配置
*   **vite.config.ts**: 
    *   已配置 `server.proxy`，将 `/api` 请求转发到后端 `http://localhost:8000`，解决跨域问题。

## 🚀 启动方式
```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev
```
